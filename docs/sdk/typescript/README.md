# CiteGenerator TypeScript SDK

This is a lightweight, fetch-based SDK meant to be copied into your project (or packaged/published).

- Entry: `docs/sdk/typescript/index.ts`
- OpenAPI: `GET /api/openapi.json`
